# Saved Results Plan

- `/saved` page reads `saju_results` for signed-in users.
- `/saved/[id]` is read-only.
- RLS protects user-specific data.
- Anonymous users cannot read saved results.
- Current page is read-only.
- No edit/delete yet.
- No AI report generation yet.
- Future steps: delete saved result, display cached AI reports, generate Pro reports.
